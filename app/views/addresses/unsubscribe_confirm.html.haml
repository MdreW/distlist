%article{id: "distlist"}  %h1.tcenter= t('devise.sessions.title')  .description.slide    = image_tag('bg/Slide.jpg')    .t4.tcenter      %span.blu= t('devise.sessions.description1').html_safe      %span.little= t('devise.sessions.description2').html_safe      %span.red= t('devise.sessions.description3').html_safe    .clear  #more_content    %h2.tcenter= t('site.addresses.title_unsubscribe')    .description      %h3= t('site.addresses.title_unsubscribed')      %p.tcenter.bold= t('site.addresses.byebye')      