%article{id: "distlist"}  %h1.tcenter= t('devise.sessions.title')  .description.slide    = image_tag('bg/Slide.jpg')    .t4.tcenter      %span.blu= t('devise.sessions.description1').html_safe      %span.little= t('devise.sessions.description2').html_safe      %span.red= t('devise.sessions.description3').html_safe    .clear  #more_content    %h2.tcenter= t('site.addresses.title_unsubscribe')    %section#import      = form_tag(unsubscribe_confirm_path(@campaign, @address.pepper, @address)) do        %fieldset          %legend= t("site.addresses.unsubscribe")          .clear            = hidden_field_tag :id, value: 1, as: :hidden            = submit_tag(I18n.t('site.addresses.unsubscribe_action'))