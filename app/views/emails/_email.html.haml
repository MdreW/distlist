%li{id: email.div, class: cycle('bdark',nil)}  = link_to email.subject.html_safe, campaign_email_path(@campaign, email)  %span{class: "fright"}= link_to( image_tag("16/destroy.png"), campaign_email_path(@campaign, email))  .details.little    - if email.logs.empty?      %dl{:class => "linear"}        %dt= image_tag( "16/mail.png")        %dd -        %dt= image_tag( "16/people.png")        %dd -              %dt= image_tag( "16/log-view.png" )        %dd -        - else      %dl{:class => "linear"}        %dt= image_tag( "16/mail.png")        %dd= l email.logs.last.created_at, format: :short        %dt= image_tag( "16/people.png")        %dd= email.logs.last.row_count        = simple_form_for([@campaign,email], :url => getlog_campaign_email_path(@campaign,email)) do |f|          %dt= f.label :idlog,  image_tag( "16/log-view.png" )          %dd= f.input_field :idlog, :collection => email.logs, :label_method => :label  .clear      